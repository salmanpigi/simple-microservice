spring:
  application:
    name: discovery-service
server:
  port: 8080
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0
  instance:
    hostname: localhost
